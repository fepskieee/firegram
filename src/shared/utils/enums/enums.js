const PENDING = "pending";
const ON_GOING = "on-going";
const DONE = "done";
const ERROR = "error";
export const STATUS = [PENDING, ON_GOING, DONE, ERROR];

const PNG = "image/png"
const JPG = "image/jpeg"
export const IMAGE_TYPES = [PNG, JPG];
export const ERROR_MSG_IMAGE_TYPE = "Please select an image file only (png or jpg)"
